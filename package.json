{
  "name": "compliance-rules-workspace",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm --prefix src/ComplianceRules.Web run build"
  }
}
